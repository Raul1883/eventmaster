# ----------------------------------------------------------------------
# Зависимости
# ----------------------------------------------------------------------
/node_modules
node_modules
/npm-debug.log
/yarn-error.log

# ----------------------------------------------------------------------
# Локальные/Чувствительные данные
# ----------------------------------------------------------------------
# 1. Секреты окружения (SESSION_SECRET и будущие ключи БД)
.env

# 2. Локальная база данных SQLite
# Ваше приложение хранит eventmaster.db в папке db
/db/eventmaster.db

# 3. Файлы сессий
# Ваш connect-sqlite3 хранит сессии в /db/eventmaster.db, 
# но если бы был отдельный файл, его тоже нужно исключить.

# 4. Логи (если будут создаваться)
logs
*.log

# ----------------------------------------------------------------------
# Docker/Контейнеры
# ----------------------------------------------------------------------
# 1. Образы, кэш и временные файлы Docker
.dockerignore
!Dockerfile
!docker-compose.yml

# 2. Результаты сборки/файлы Docker Compose
*.swp
authorized_key.json